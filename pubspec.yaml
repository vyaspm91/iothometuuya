name: iothometuuya
description: Smart Home enpowerd with Tuya SDK.


publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  provider: ^4.3.1
  http: ^0.12.1
  shared_preferences: ^0.5.8
  bloc: ^7.0.0
  flutter_bloc: ^7.0.0
  equatable: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0
  bluetooth_enable: ^0.1.1

flutter:

  uses-material-design: true

